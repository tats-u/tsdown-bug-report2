{
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "build:test": "pnpm -r build && pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsdown": "^0.14.2",
    "typescript": "^5.9.2"
  },
  "packageManager": "pnpm@10.6.4"
}
